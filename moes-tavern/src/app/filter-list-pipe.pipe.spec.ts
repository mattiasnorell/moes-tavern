import { FilterListPipePipe } from './filter-list-pipe.pipe';

describe('FilterListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
